#!/bin/bash
export HASHES_FILE=~/.kopiaindex/hashes.sorted
export INDEX_FILE=~/.kopiaindex/filenames.sorted
# TODO: URL encode the search terms
ENCODED="$*"
look "${ENCODED}" "${INDEX_FILE}" | awk '{ print $2 }' | sort | /usr/bin/join - "${HASHES_FILE}"
